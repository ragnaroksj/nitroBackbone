/*:nodoc:*
 * class ActionAppend
 *
 * This action stores a list, and appends each argument value to the list.
 * This is useful to allow an option to be specified multiple times.
 * This class inherided from [[Action]]
 *
 **/"use strict";var util=require("util"),Action=require("../action"),$$=require("../const"),ActionAppend=module.exports=function(t){t=t||{};if(this.nargs<=0)throw new Error("nargs for append actions must be > 0; if arg strings are not supplying the value to append, the append const action may be more appropriate");if(!!this.constant&&this.nargs!==$$.OPTIONAL)throw new Error("nargs must be OPTIONAL to supply const");Action.call(this,t)};util.inherits(ActionAppend,Action);ActionAppend.prototype.call=function(e,t,n){var r=[].concat(t[this.dest]||[]);r.push(n);t.set(this.dest,r)};