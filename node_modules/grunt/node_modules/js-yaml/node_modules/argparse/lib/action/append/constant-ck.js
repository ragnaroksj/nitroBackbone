/*:nodoc:*
 * class ActionAppendConstant
 *
 * This stores a list, and appends the value specified by
 * the const keyword argument to the list.
 * (Note that the const keyword argument defaults to null.)
 * The 'appendConst' action is typically useful when multiple
 * arguments need to store constants to the same list.
 *
 * This class inherited from [[Action]]
 **/"use strict";var util=require("util"),Action=require("../../action"),ActionAppendConstant=module.exports=function(t){t=t||{};t.nargs=0;if(t.constant===undefined)throw new Error("constant option is required for appendAction");Action.call(this,t)};util.inherits(ActionAppendConstant,Action);ActionAppendConstant.prototype.call=function(e,t){var n=[].concat(t[this.dest]||[]);n.push(this.constant);t.set(this.dest,n)};